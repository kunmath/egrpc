// egrpc grpcpp shim — upstream pushes platform macros (e.g. Windows'
// min/max) here and pops them in ports_undef.inc; egrpc targets Linux only
// (design §2) and needs no macro juggling, so this is intentionally empty.
